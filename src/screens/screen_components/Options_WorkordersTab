import {
  View,
  Text,
  Pressable,
  TextInput,
  FlatList,
  TouchableOpacity,
} from "react-native-web";
import { cloneDeep } from "lodash";

export function OptionsWorkorderTab({ workordersArr }) {
  return <View style={{ width: "100%" }}></View>;
}
